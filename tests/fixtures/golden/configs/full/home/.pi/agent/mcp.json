{
  "mcpServers": {
    "codebase-memory": {
      "command": "codebase-memory-mcp",
      "args": []
    },
    "axiom": {
      "command": "mcp-remote",
      "args": [
        "https://mcp.axiom.co/mcp",
        "--allow-http",
        "--header",
        "Authorization: Bearer ${AXIOM_TOKEN}"
      ]
    }
  }
}
