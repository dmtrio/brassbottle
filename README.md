# pr-screenshots

Orphan branch holding UI captures used as Product Evidence on pull requests.
Never merged. Layout: `pr-<number>/<candidate-sha>/<name>.png`. PR comments
link each image at a pinned commit of this branch, so a capture stays bound
to the Candidate it was taken from. Captures use test fixtures only.
